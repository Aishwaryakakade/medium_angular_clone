export interface CurrentUserInterface {
  email: string;
  token: string;
  username: string;
  bio: string | null;
  image: string | null;
}

//Intreface for user that can be usee accross the whole application
